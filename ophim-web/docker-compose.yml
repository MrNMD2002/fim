services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    # Tùy chọn: override API nguồn
    # env_file: .env
    # - OPHIM_BASE_URL=https://ophim1.com
    # - IMAGE_BASE_URL=https://img.ophim.live
